import { expect } from 'chai'

import {PlayerPojo, RoomPojo} from '../src/ts/types/app_types'
import { Direction } from '../src/ts/enums/app_enums'
import characters from '../src/resources/characters'
import Player from '../src/models/player/Player'
import Room from '../src/models/room/Room'

describe('Game', () => {
  describe('initialize room', () => {

    it('should place the player inside the room', () => {
//      const r: RoomPojo = rooms[1];
//      const room: Room = Room.createFromPojo(r);
//      const c: PlayerPojo = characters[1];
//      const player: Player = Player.createFromPojo(c);
    })
  })
})
